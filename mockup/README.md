# Mockup Reference

Drop design files here. The `ui-component` skill reads from this folder when porting screens to `apps/web/`.

Accepted formats: `.html`, `.jsx`, `.tsx`, `.css`, `.png`, `.fig`, `.pdf`

Naming convention: `screen-name.ext` (e.g. `dashboard.html`, `case-review.jsx`)
