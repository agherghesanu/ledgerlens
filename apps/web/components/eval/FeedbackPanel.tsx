// Per-dimension LLM rationale and improvement suggestions
export function FeedbackPanel({ reviewId }: { reviewId: string }) {
  return (
    <div className="rounded-lg border p-6">
      <h2 className="font-semibold mb-4">Feedback</h2>
      {/* TODO: fetch feedback, render accordion per dimension */}
    </div>
  );
}
